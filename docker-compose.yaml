services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: document_cache
      POSTGRES_PASSWORD: document_cache
      POSTGRES_DB: document_cache
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

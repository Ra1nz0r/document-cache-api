info:
  name: Заголовки одного документа
  type: http
  seq: 8

http:
  method: HEAD
  url: "{{url}}:{{port}}/api/docs/{{document_id}}?token={{token}}"
  params:
    - name: token
      value: "{{token}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
